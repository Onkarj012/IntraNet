# Data Directory

This repository tracks only small reference CSVs under:

- `data/prices/`
- `data/sentiment/`

These files exist to document expected input formats and support lightweight experimentation.

Large raw datasets, cached downloads, minute-bar archives, and generated research outputs should remain local and are ignored by git.
